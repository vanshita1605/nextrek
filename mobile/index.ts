import { registerRootComponent } from 'expo';
import App from './App';

registerRootComponent(App);

// Using WebView-backed mobile container to show the web app at http://10.0.2.2:3000 (Android) or http://localhost:3000 (iOS).
// If you prefer native app routing, run the separate workspace at mobile/nextrek.

